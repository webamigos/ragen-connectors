[build]
builder = "DOCKERFILE"
dockerfilePath = "services/rejestrio/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
numCpus = 1
memoryMB = 1024

/**
 * The shared helpers, vendored.
 *
 * The workspace build of this file re-exports `@ragen-connectors/core`. A
 * standalone connector cannot: core is not published to npm, and deliberately
 * — it carries `RagenVaultClient`, an HMAC-signed contract with an internal
 * Ragen service that a connector outside that deployment has no secret for and
 * no business holding.
 *
 * So the four helpers that are not the vault client live here. `env.ts` and
 * `customer.ts` are byte-identical copies of core's; `logger.ts` is a smaller
 * reimplementation and says why in its own header.
 *
 * Vendored from @ragen-connectors/core — generated by create-ragen-connector@__CLI_VERSION__.
 */

export { validateEnvVars, validateEnv } from "./env.js";
export { logger } from "./logger.js";
export { getCustomerId } from "./customer.js";

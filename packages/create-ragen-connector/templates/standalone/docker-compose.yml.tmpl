services:
  __SLUG__:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      # Hono, then MCP. The second is the one Ragen dials.
      - "__PORT__:__PORT__"
      - "__MCP_PORT__:__MCP_PORT__"
    env_file:
      - .env.local
    restart: unless-stopped

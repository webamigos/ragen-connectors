{
  "name": "__SLUG__-mcp",
  "version": "0.1.0",
  "description": "__DESCRIPTION__",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch --env-file=.env.local src/index.ts",
    "start": "node --env-file=.env.local dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.11",
    "fastmcp": "^3.34.0",
    "hono": "^4.12.8",
    "pino": "^10.3.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.0.0",
    "eslint": "^10.0.3",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.57.0",
    "vitest": "^4.1.1"
  },
  "engines": {
    "node": ">=24"
  },
  "//packageManager": "npm 10's dependency resolver crashes on vitest's peer set here (`Cannot read properties of null (reading 'edgesOut')`), which reads as a broken template rather than a broken npm. Pinned so corepack fetches one that works.",
  "packageManager": "npm@11.19.0"
}
